<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.TbGoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.domain.entity.TbGoods">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="img" property="img" />
        <result column="class_id" property="classId" />
        <result column="price" property="price" />
        <result column="bar_code" property="barCode" />
        <result column="measure" property="measure" />
        <result column="inventory" property="inventory" />
        <result column="detail" property="detail" />
        <result column="is_del" property="isDel" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, img, class_id, price, bar_code, measure, inventory, detail, is_del, create_time, update_time
    </sql>

</mapper>
